{
  "rules": {
    "events": {
      "$eventId": {
        ".read": true,
        ".write": "auth != null && auth.token.admin === true",
        "participants": {
          ".indexOn": ["createdAt"]
        },
        "waitlist": {
          ".indexOn": ["createdAt"]
        }
      }
    }
  }
}
